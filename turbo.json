{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["src/**"]
    },
    "//#migrate": {},
    "//#lint:ci": {},
    "//#lint": {},
    "start": {
      "dependsOn": ["build", "//#migrate"],
      "cache": false
    },
    "dev": {
      "dependsOn": ["build", "//#migrate"],
      "cache": false

    }
  }
}

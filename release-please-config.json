{
  "bootstrap-sha": "d4f4923d576d7777ff08bdc43c628aecc4a0d33f",
  "release-type": "node",
  "pullRequestTitlePattern": "chore: release ${component} ${version}",
  "group-pull-request-title-pattern": "chore: release ${component} ${version}",
  "packages": {
    ".": {
      "component": "@quack/quack"
      "pullRequestTitlePattern": "chore: release ${component} ${version}",
      "group-pull-request-title-pattern": "chore: release ${component} ${version}"
    },
    "packages/app": {
      "component": "@quack/app"
      "pullRequestTitlePattern": "chore: release ${component} ${version}",
      "group-pull-request-title-pattern": "chore: release ${component} ${version}"
    },
    "packages/server": {
      "component": "@quack/server"
      "pullRequestTitlePattern": "chore: release ${component} ${version}",
      "group-pull-request-title-pattern": "chore: release ${component} ${version}"
    },
    "packages/rpc": {
      "component": "@quack/rpc"
      "pullRequestTitlePattern": "chore: release ${component} ${version}",
      "group-pull-request-title-pattern": "chore: release ${component} ${version}"
    }
  },
  "plugins": [
    {
      "type": "linked-versions",
      "groupName": "@quack/quack",
      "merge": true,
      "components": [
        "@quack/quack",
        "@quack/app",
        "@quack/server",
        "@quack/rpc"
      ]
    }
  ]
}

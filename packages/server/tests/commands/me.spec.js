/* eslint-disable no-unused-vars */
const assert = require('assert');
const { db } = require('../../src/infra/repositories');

module.exports = (connect) => {
  describe('/me', () => {
    it('should display information about user');
  });
};
